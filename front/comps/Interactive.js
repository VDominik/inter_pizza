import Link from "next/link";
import styles from "../styles/Home.module.css";

const Navbar = () => {
    return(      <div className={styles.interactive} id="interactive">
            <h1>Interactive</h1>
        </div>
    );
}
export default Navbar;